---
title: Immer
description: このドキュメントは Immer 拡張について説明します。
nav: 4.04
keywords: immer
---

### インストール

この機能を使用するには `immer` と `jotai-immer` をインストールする必要があります。

```
npm install immer jotai-immer
```

## atomWithImmer

`atomWithImmer` は通常の [`atom`](../core/atom.mdx) に似た新しいアトムを作成しますが、異なる `writeFunction` を持ちます。このバンドルには読み取り専用アトムはありません。これらの関数の目的は immer の produce（ミュータビリティ）関数だからです。
writeFunction のシグネチャは `(get, set, update: (draft: Draft<Value>) => void) => void` です。

```jsx
import { useAtom } from 'jotai'
import { atomWithImmer } from 'jotai-immer'

const countAtom = atomWithImmer({ value: 0 })

const Counter = () => {
  const [count] = useAtom(countAtom)
  return <div>count: {count.value}</div>
}

const Controls = () => {
  const [, setCount] = useAtom(countAtom)
  // setCount === update : (draft: Draft<Value>) => void
  const inc = () =>
    setCount((draft) => {
      ++draft.value
    })
  return <button onClick={inc}>+1</button>
}
```

### 例

atomWithImmer を使った例：

<Stackblitz id="vitejs-vite-tblppw" file="src%2FApp.tsx" />

## withImmer

`withImmer` はアトムを受け取り派生アトムを返します。`atomWithImmer` と同様に異なる `writeFunction` を持ちます。

```jsx
import { useAtom, atom } from 'jotai'
import { withImmer } from 'jotai-immer'

const primitiveAtom = atom({ value: 0 })
const countAtom = withImmer(primitiveAtom)

const Counter = () => {
  const [count] = useAtom(countAtom)
  return <div>count: {count.value}</div>
}

const Controls = () => {
  const [, setCount] = useAtom(countAtom)
  // setCount === update : (draft: Draft<Value>) => void
  const inc = () =>
    setCount((draft) => {
      ++draft.value
    })
  return <button onClick={inc}>+1</button>
}
```

### 例

withImmer を使った例：

<Stackblitz id="vitejs-vite-jwfjqm" file="src%2FApp.tsx" />

## useImmerAtom

このフックはアトムを受け取り、アトムの `writeFunction` を以前のヘルパーと同様の immer ライクな `writeFunction` に置き換えます。

```jsx
import { atom } from 'jotai'
import { useImmerAtom } from 'jotai-immer'

const primitiveAtom = atom({ value: 0 })

const Counter = () => {
  const [count] = useImmerAtom(primitiveAtom)
  return <div>count: {count.value}</div>
}

const Controls = () => {
  const [, setCount] = useImmerAtom(primitiveAtom)
  // setCount === update : (draft: Draft<Value>) => void
  const inc = () =>
    setCount((draft) => {
      ++draft.value
    })
  return <button onClick={inc}>+1</button>
}
```

`withImmer` と `atomWithImmer` は immer ライクな `writeFunction` を提供するため、新しいものを作成する必要がない `useImmerAtom` と組み合わせないほうが良いでしょう。

セッター部分のみ必要な場合は `useSetImmerAtom` を使用できます。

### 例

useImmerAtom を使った例：

<Stackblitz id="vitejs-vite-k2rixl" file="src%2FApp.tsx" />

## デモ

<Stackblitz id="vitejs-vite-tksbwq" file="src%2FApp.tsx" />
